(()=>{const API=()=>localStorage.getItem('aither-backend-url')||'https://aitherbackendnew.onrender.com';const escA=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));const button=document.createElement('button');button.id='aitherAccountButton';button.type='button';button.setAttribute('aria-label','Account');button.title='Aither Account';button.textContent='●';document.querySelector('.toolbar')?.appendChild(button);const modal=document.createElement('div');modal.className='account-modal hidden';modal.innerHTML='<div class="account-card"><div class="account-head"><div><div class="account-kicker">AITHER ACCOUNT</div><h2>Aither Account</h2></div><button class="account-close" type="button">×</button></div><div id="accountBody"></div></div>';document.body.appendChild(modal);const body=modal.querySelector('#accountBody');let mode='login',current=null;async function request(path,options={}){const r=await fetch(API()+path,{...options,credentials:'include',headers:{'Content-Type':'application/json',...(options.headers||{})}});let data={};try{data=await r.json()}catch{}if(!r.ok)throw Error(data.detail||'Aither Backend request failed.');return data}function render(){if(current){body.innerHTML=`<div class="account-user"><strong>${escA(current.name)}</strong><small>${escA(current.email)}</small></div><div class="account-actions"><button class="account-secondary" id="accountLogout" type="button">Log out</button></div>`;body.querySelector('#accountLogout').onclick=logout;return}body.innerHTML=`<form class="account-form" id="accountForm">${mode==='register'?'<label>Name<input name="name" autocomplete="name" required maxlength="80"></label>':''}<label>Email<input name="email" type="email" autocomplete="email" required></label><label>Password<input name="password" type="password" autocomplete="${mode==='register'?'new-password':'current-password'}" minlength="8" required></label><button class="account-submit" type="submit">${mode==='register'?'Create account':'Sign in'}</button></form><div class="account-status" id="accountStatus"></div><button class="account-switch" id="accountSwitch" type="button">${mode==='register'?'Already have an account? Sign in':'Need an account? Create one'}</button>`;body.querySelector('#accountForm').onsubmit=submit;body.querySelector('#accountSwitch').onclick=()=>{mode=mode==='register'?'login':'register';render()}}async function submit(e){e.preventDefault();const form=e.currentTarget,status=body.querySelector('#accountStatus'),data=Object.fromEntries(new FormData(form));status.textContent='Connecting to Aither Backend…';try{const result=await request(mode==='register'?'/api/auth/register':'/api/auth/login',{method:'POST',body:JSON.stringify(data)});current=result.user;render()}catch(err){status.textContent=err.message}}async function session(){try{const r=await request('/api/auth/session');current=r.authenticated?r.user:null}catch{current=null}render()}async function logout(){try{await request('/api/auth/logout',{method:'POST'})}catch{}current=null;render()}button.onclick=()=>{modal.classList.remove('hidden');session()};modal.querySelector('.account-close').onclick=()=>modal.classList.add('hidden');modal.addEventListener('click',e=>{if(e.target===modal)modal.classList.add('hidden')});render();session()})();